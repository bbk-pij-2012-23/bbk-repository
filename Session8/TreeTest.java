import org.junit.*;

public class TreeTest {

/** methods need to do the folowing:
* add() creates a root with the mid value 
* add() node added is corrected pointed at by parent node
* add() puts > ints on the right and < ints on left
* remove() makes parent point to grandchild
* remove() makes int a parent to its sibling
* find() uses a boolean to check element is target int
* find() checks each element systematically ruling out branches as approp
* not sure how to test for that
*/
